function log( msg ) {
  msg asm{println # undefined};
}

function criaObj() {
  return {  b: 'nome'};
}

let obj = [];
let c;
{ obj = criaObj();
let c = [];

log( obj );
log( obj.a );
log( obj.a[obj.a[1].teste+1] );
}